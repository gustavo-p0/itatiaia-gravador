services:
  - type: web
    name: itatiaia-gravador
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GOOGLE_SERVICE_ACCOUNT_KEY
        sync: false
      - key: AUDD_API_KEY
        sync: false
